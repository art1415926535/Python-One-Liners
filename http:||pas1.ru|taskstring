#   Вводится строка слов. Вывести слова в обратном порядке. 
print([x for x in input().split()].reverse())


#  Отсортировать массив строк по алфавиту. Учитывать только первый символ каждой строки. 
print(' '.join(sorted([x for x in input().split()], key=lambda y: y[0])))

#  Заменить пробел и группы пробелов символом "*". 
#  Дана строка слов, разделенных пробелами. Между словами может быть несколько пробелов.
print('*'.join(input().split()))


#  Найти слово, стоящее в тексте под определенным номером, и вывести его первую букву. 
print(input('Слова:').split()[int(input('Номер:'))])


#  Заменить в самом длинном слове строки буквы 'a' на 'b'
print(sorted([x for x in input().split()], key=lambda x: len(x))[-1].replace('a', 'b'))


#  Определить длину самого короткого слова в строке
print(len(sorted([x for x in input().split()], key=lambda x: len(x))[0]))


#  Заменить три последних символа у слов, больших определенной длины
#  Дан массив слов. Заменить последние три символа слов, имеющих выбранную длину на символ "$".
print(' '.join(list(map(lambda (x, y): x if len(x) < y else x[:-3]+'$$$', zip([x for x in input('Слова: ').split()], [int(input('Размер: '))]*99)))))
